# django-pip-example

Репозиторий для демонстрации деплоя проекта на Python с фреймворком Django и пакетным менеджером pip

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py runserver
```
